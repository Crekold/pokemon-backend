# Configuración del datasource para MySQL
spring.datasource.url=jdbc:mysql://localhost:3306/pokemon_db?serverTimezone=GMT-4
spring.datasource.username=root
spring.datasource.password=admin
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Configuración del JPA para MySQL
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=none

# Configuración de la zona horaria de La Paz
spring.jackson.time-zone=GMT-4

# Sin autenticación
spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.security.servlet.SecurityAutoConfiguration

# Configuracion del puerto
server.port=3030